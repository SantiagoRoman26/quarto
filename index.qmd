
---
title: "PRIMER REPORTE EN QUARTO"
author: "Yachay tech University"
date: "25-09-2025"
format: html
---

# titulo 1
## subtitulo

```{python}
import altair as alt
from vega_datasets import data
cars = data.cars()
print(cars.head())
```

## Visualizacion en altair
```{python}

alt.Chart(cars).mark_point().encode(
    x="Miles_per_Gallon",
    y="Horsepower",
    tooltip=["Name","Origin","Miles_per_Gallon","Horsepower"]
).interactive()

```
### Visualizacion de Scatterplot con color

```{python}
alt.Chart(cars).mark_point(filled=True).encode(
    x="Miles_per_Gallon",
    y="Horsepower",
    color="Origin",
    tooltip=["Name", "Origin", "Miles_per_Gallon", "Horsepower"]
).interactive()
```

-lista 
    -elemento 1
    -elemento 2